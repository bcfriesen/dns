apiVersion: apps/v1
kind: Deployment
metadata:
  name: pdns-recursor-internal
  namespace: dns
  labels:
    app.kubernetes.io/name: pdns-recursor-internal
    app.kubernetes.io/component: recursor-internal
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: pdns-recursor-internal
      app.kubernetes.io/component: recursor-internal
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  template:
    metadata:
      labels:
        app.kubernetes.io/name: pdns-recursor-internal
        app.kubernetes.io/component: recursor-internal
    spec:
      serviceAccountName: pdns
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: pdns-recursor-internal
      securityContext:
        runAsNonRoot: true
        runAsUser: 953
        runAsGroup: 953
        fsGroup: 953
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: pdns-recursor-internal
          image: powerdns/pdns-recursor-50:5.0.4
          ports:
            - name: dns-udp
              containerPort: 5301
              protocol: UDP
            - name: dns-tcp
              containerPort: 5301
              protocol: TCP
          volumeMounts:
            - name: config
              mountPath: /etc/powerdns/recursor.conf
              subPath: recursor.conf
              readOnly: true
            - name: config
              mountPath: /etc/powerdns/pdns-recursor.lua
              subPath: pdns-recursor.lua
              readOnly: true
            - name: tmp
              mountPath: /tmp
            - name: var-run
              mountPath: /var/run/pdns-recursor
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          resources:
            requests:
              cpu: 500m
              memory: 512Mi
            limits:
              cpu: 2000m
              memory: 1Gi
          livenessProbe:
            exec:
              command:
                - rec_control
                - ping
            initialDelaySeconds: 15
            periodSeconds: 20
            failureThreshold: 3
          readinessProbe:
            exec:
              command:
                - rec_control
                - ping
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
      volumes:
        - name: config
          configMap:
            name: pdns-recursor-internal-config
        - name: tmp
          emptyDir: {}
        - name: var-run
          emptyDir: {}
