# In production, replace this with an ExternalSecret or Sealed Secret.
# Plaintext values are placeholders only.
apiVersion: v1
kind: Secret
metadata:
  name: pdns-auth-secret
  namespace: dns
  labels:
    app.kubernetes.io/name: pdns-auth
    app.kubernetes.io/component: authoritative
type: Opaque
stringData:
  PDNS_DB_HOST: "pdns-postgres-rw.dns.svc.cluster.local"
  PDNS_DB_NAME: "pdns"
  PDNS_DB_USER: "pdns"
  PDNS_DB_PASSWORD: "CHANGEME"
  PDNS_API_KEY: "CHANGEME"
